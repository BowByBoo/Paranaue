[package]
name = "forge"
version = "0.1.0"
edition = "2024"
description = "A small, native, extensible command shell built as the foundation of the Forge computing project."
license = "MIT"

[dependencies]
rustyline = "18.0.1"
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.4"
